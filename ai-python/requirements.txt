fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
google-generativeai>=0.8.3
# Upper-bounded: an unbounded openai let a fresh install silently jump to a
# new major release that dropped its direct httpx dependency, breaking
# test_llm_reliability.py's import with no code change on our side. Bump the
# bound deliberately, not by drift.
openai>=1.55.0,<3.0.0
# Used directly by test_llm_reliability.py (it builds an httpx.Request to
# simulate a provider timeout) — previously relied on riding in transitively
# via openai's own dependency, which is exactly what broke above.
httpx>=0.27.0

prometheus-client>=0.20.0
