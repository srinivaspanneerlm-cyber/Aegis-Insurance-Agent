/** Shared types for the admin dashboard and its viewport components. */

/** The selectable admin dashboard sections. */
export type AdminNav =
  | "analytics"
  | "leads"
  | "chats"
  | "vault"
  | "automation"
  | "telemetry";
