/**
 * Theme-derived class strings for the About page sections.
 *
 * Both helpers the About sections use — `wrapperClass` and `glassCardClass` —
 * are shared verbatim with other pages, so they live in the shared theme
 * module and are re-exported here to keep the section imports stable.
 */
export { wrapperClass, glassCardClass } from "@/components/shared/themeClasses";
